services:
  db:
    image: postgres:16
    container_name: healingmindset-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: hackmeBABYonemoretime9898!
      POSTGRES_DB: healingmindset_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: